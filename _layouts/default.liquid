<!DOCTYPE html>
<html>
    <head>
        {% include "head.liquid" %}
    </head>
    <body>
      {% include "header.liquid" %}
      <main>
        <h2>{{ page.title }}</h2>
        {{ page.content }}
      </main>
  </body>
</html>
